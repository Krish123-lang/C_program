#include <stdio.h>
int main()
{
    int n, i = 1;
    printf("Enter number: ");
    scanf("%d", &n);

    // For loop
    /*
    for (i = 1; i <= 10; i++)
    {
        printf("%d x %d = %d\n", n, i, n * i);
    }
    */

    // ************************************************************************************************************
    /*
     while (i <= 10)
     {
         printf("%d x %d = %d\n", n, i, n * i);
         i++;
     }
     */
    // ************************************************************************************************************

    do
    {
        printf("%d x %d = %d\n", n, i, n * i);
        i++;
    } while (i <= 10);
    // ************************************************************************************************************

    return 0;
}