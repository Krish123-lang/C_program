#include <stdio.h>
#include <math.h>
int main()
{
    /*
    double sinm = sin(45.4);
    printf("%lf", sinm);
    */

    /*
     int expm = exp(4);
     printf("%d", expm); // Output: 54
    */

    /*
        int powm = pow(4, 2);
        printf("%d", powm); // 16
    */

    /*
     int sqrtm = sqrt(4);
     printf("%d", sqrtm); // 2
     */

    /*
    sin (double);
    cos (double);
    tan (double);
    sinh (double);
    cosh (double);
    tanh (double);
    asin (double);
    acos (double);
    atan (double);
    atan2 (double, double);
    exp (double);
    log (double);
    log10 (double);
    pow (double, double);
    sqrt (double);
    ceil (double);
    floor (double);
    fabs (double);
    ldexp (double, int);
    frexp (double, int*);
    modf (double, double*);
    fmod (double, double);
    */
    return 0;
}